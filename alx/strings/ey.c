#include <stdio.h>
#include <string.h>

/**
 * main - this is a program
 * Description - this is a program for strings
 *
 * Return: 0
 */

int main()
{
	char i [] = "trudeau";

	int lent;

	lent = strlen(i);

	printf("this is a good %d\n", lent);
	return (0);
}
